# Frontend Environment Variables

# The base URL of your backend API
# Backend runs on port 8083 (configured in application.properties):
VITE_API_URL=http://localhost:8083/api

# Additional frontend configuration (if added later)
# VITE_APP_NAME="Secure Cloud Storage"
